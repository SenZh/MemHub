---
description: "ExoBrain 自动化知识沉淀与避坑检索规则"
globs: "*"
alwaysApply: true
---

# ExoBrain 知识外脑协作规则

你已连接本地知识外脑服务（ExoBrain MCP）。请在工程协作中严格遵守以下指引：

## 1. 遭遇复杂排错或构思方案前（主动避坑）
在排查疑难 Bug、环境配置问题或技术方案选型前，优先调用 `exo_search_knowledge(query)` 搜索过往避坑指南。
- 若命中历史卡片，调用 `exo_get_knowledge(id)` 查看完整的根因与正解，防止重复踩坑。

## 2. 攻克难题或用户提示沉淀时（主动沉淀）
当解决了复杂排错、做出了重要架构决策，或者用户输入 `/know`、`/save` 或说“记录/总结一下刚才的方案”时：
- 请提炼核心经验，并立即调用 MCP 工具 `exo_record_knowledge` 将其落盘！
- 标题严格规范：`[技术栈/组件] 核心场景/症状 最终结论/正解 (20-35字)`。
- 不要只在聊天框里文字总结，必须调用工具落盘。
