# BATTLE 轮报告：架构正确性评审员（A）互评表态

> 变更编号：2026-09-24_memhub-opencode-v1v2-adapter
> 角色：架构正确性评审员（A，Architecture Correctness）
> 轮次：BATTLE 互评轮（读 review-risk.md / review-test.md / battle-risk.md / battle-test.md + 第三轮真机 F13/F14/F15）
> 对象：`review-arch.md`（我的原报告）、`tech-design.md`（v2 方案）、`verification.md`（真机）
> 铁律遵守声明：凡与真机实测冲突的主张一律让步；无证据断言降级或撤回；禁止编造。本轮所有源码行号均**亲自复读**确认。

---

## ① 逐条表态表

### 1.1 对我自己原报告的 3 个 P0

| 编号 | 议题 | 我的表态 | 依据 / 修正 |
|---|---|---|---|
| **P0-A** | 信号1（idle 消息）无落地点 | **[坚持]**，但**接受与 C-§4 合并**，并**接受 B 的补充切面** | 复读 `opencode-client.js:623`：`entry.type === 'assistant'` 是 `getLastAssistantProgress` 唯一识别分支，全文无 `type==='idle'`；复读 `:670`：信号1 现为 `getSessionStatus` + 短路。**v2 §4.2 :100 的"信号1=读消息的 idle 消息"在 `waitForSessionIdle` 内确无接入点**。此事实三方零分歧（C §0 表第 2 行亲验、B ②.4 亲验）。**合并 1 条**。 |
| **P0-B** | `failed`/`interrupted` outcome 未区分 | **[坚持]** | 复读 `tech-design.md:100-101`：原文「exist and outcome 已落定（require outcome ∈ {succeeded, failed, interrupted}）」——三态**等价用于"完成"**；复读 `daemon.js:468`：`if (waited.completed)` 真即打印「✅ 复盘完成」，随后 `:475-479` 无条件置 EXTRACTED。**A 侧证据链完整**。B ②.3 已 **[接受对方]** 并自我修正其原判过宽；C 1.1 已 **[接受对方]** 并与其 R11-2 合并。**维持 P0**。 |
| **P0-C** | `order+cursor` 违背契约 | **[坚持] + 由 F13 升级为"已确证"** | F13 实测 `order=asc&cursor=<c>` → **400 `InvalidCursorError: "Cursor cannot be combined with order"`**。我原报告结论（P0）**被真机证实**，但**我原述措辞"可能失效/未验证"严重低估**——F13 证明是**确定失效**（第二页即 400）。**自我修正措辞：从"违背 spec、未验证"升级为"真机硬拒绝、方案唯一分页手段作废"**。 |

### 1.2 对 B（review-risk / battle-risk）的表态

| B 条目 | 我的表态 | 依据（含本轮亲自复读） |
|---|---|---|
| **B 的 P0-1**（V1 分页回归） | **[接受对方]，并承认这是我 A 侧的独立遗漏** | 见 §②.2。我通读自己的 review-arch.md（200 行）确认：§4.3 相关讨论**仅在** P0-C（order+cursor 互斥）与 P1-E（`listCandidateSessions`），**全文零处提及 V1 消息端点裸数组的翻页兼容**；R1–R13 覆盖表 R2 行（:77）亦只指向 P0-C/P1-E。**这是我的真实盲区，不辩解。** |
| **B 的 P0-2(a)**（改 unknown 后 V2 永不返回 idle） | **[接受对方]，确认与我的 P1-D 同源** | 见 §②.3。 |
| **B 的 P0-2(b)**（超时即 EXTRACTED） | **[接受对方]**，作为独立 P0 保留；**并采纳 B 的自我修正（撤回 25 分钟级联）** | 复读 `daemon.js:234`：`const limit = cliOptions.limit || 1;` —— **确认为 1**，B 的自我修正成立，其"5×5=25min 调度拥塞"确为编造性推断，**已撤回，我认可撤回**。但 `daemon.js:468-479`（`else{warn}` 后仍无条件 EXTRACTED）+ `:487-493`（finally 无条件删 fork）**定性成立**。 |
| **B 的 P1（asc+cursor 未验证）** | **[接受对方] 升 P0**，与我的 P0-C 去重合并 | F13 已确证。B battle §②.5 的合并处置正确。 |
| **B 的 R8（凭据外发）** | **[接受对方]，见 §②.6 的裁决** | — |
| **B 的 P2（dry-run 误导 URL、listCandidate 翻页开销）** | **[接受对方]** | 我原报告未覆盖 dry-run 误导 URL，B 补充正确。 |

### 1.3 对 C（review-test / battle-test）的表态

| C 条目 | 我的表态 | 依据 |
|---|---|---|
| **C 的 §4（R11-4：信号1/信号2 布尔关系未定义）** | **[接受对方]，与我的 P0-A 合并**——且**承认 C 比我更精确** | 见 §②.1 的精确性裁决。C 复读 `:625` 确认现码为 `completed \|\| streamed`（OR）；我原 P0-A 只论证"信号1 无落地点"，**未论证"接入后如何与信号2 组合"**。两点是同一缺口的两段：A 管"接线"，C 管"接线后的语义"。合并后更完整。 |
| **C 的 §5c（fork 字段未真机验证）** | **[接受 F14，缺口关闭]** | F14 实测列表元素带 `fork={"sessionID":...}`。C 已自我修正降 P2。我原报告 P2-H 提到的"DB 路径无 fork 字段"**不受影响，仍成立**（F14 只解决 HTTP 列表路径，`adapters/opencode.js:97` 走 SQLite `parent_id`，仍无 fork）。 |
| **C 的 §7（异常路径零断言）升 P0** | **[接受对方升至 P0]**，见 §②.5 | 复读 `opencode-client.js:576`（非 ok 抛错）→ `:666-668`（`catch { status='unknown' }` 吞掉）→ 走超时。链路成立。 |
| **C 的 §8a（既有测试冲突，F15）** | **[接受对方升至 P0]**，见 §②.5 | 复读 `test-host-client-v2.js:152`（断言 `'idle'`）、`:198-199`（断言 `completed:true, finalStatus:'idle'`）与真机 F10/F11 逐字吻合。 |
| **C 对 R4/R12 的降级** | **[接受对方的部分]**：R12 接受合并为 P0（异常路径，见 §②.5）；**R4 我维持"有处置但未落地"**——C 在 battle 中降为 P1，**我原判"✅ 有处置"过于宽松，接受 C 的"承诺不具体"**，降 P1。 |

### 1.4 对真机 F13/F14/F15 的表态

| 真机事实 | 我的表态 | 影响 |
|---|---|---|
| **F13**（order+cursor → 400） | **[让步 + 升级]**：我的 P0-C 从"spec 推断"升级为"真机确证"；且**我原述"可能失效"是错误低估**，实为**确定失效** | 见 §②.4。 |
| **F14**（列表元素带 fork） | **[让步]**：C 的 5c 缺口关闭；**我的 P2-H（DB 路径）不受影响** | — |
| **F15**（mock 复刻错误行为，修复必打红） | **[接受]**：支持 C 的 §8a 升 P0 | — |

---

## ② 我的原 P0 去留裁决（逐条）

### ②.1 P0-A 与 C-§4 的精确性裁决 —— **【合并，C 更精确，A 更定位】**

**任务书问：C 的 §4「信号1/信号2 布尔关系未定义」是否比我的 P0-A「无落地点」更精确？**

**裁决：两者精确性维度不同，不可互相替代，必须合并；C 在"设计自洽性"维度更精确，A 在"代码落点"维度更精确。**

- **我的 P0-A 的精确性**：定位到**函数级**——信号1 应为的"读消息判 idle"在 `waitForSessionIdle`（:651-697）中**无任何代码行**；现码信号1 实为 `:665`（`getSessionStatus`）+ `:670`（短路），信号2 实为 `:676`（`getLastAssistantProgress`）。→ 给实现者的信息是「**你打算把这段逻辑写在哪个函数里？**」
- **C 的 §4 的精确性**：定位到**语义级**——`tech-design.md:100`（信号1 主）与 `:102`（信号2 辅）**未定义布尔关系**；叠加现码 `:625` 的 `completed || streamed`（OR），若"辅信号"被实现为 OR，则 **R3（streamed 误判）原样复活**，而 v2 :150 声称"已移除 streamed，风险消解"**不成立**。→ 给实现者的信息是「**你打算怎么组合这两个信号？AND 还是 OR？**」
- **判定**：`P0-A ⊆ (A + C 合并条)`。**C 的补充不是"更精确"，而是"发现了 A 未覆盖的第二段缺口"**。我原报告的"不够具体"措辞（review-arch.md:25 建议列）**只说了"写清接入点"，未要求"裁决布尔关系"**——此为我校准后的**补充承认**。

**合并后表述（三方共识版）**：
> **v2 §4.2「完成判定」不可落地且内部矛盾**：① 信号1（idle 消息）在 `waitForSessionIdle` 中**无接入点**（现码信号1 是 `getSessionStatus`+:670 短路；`:623` 的 `getLastAssistantProgress` 只认 `type==='assistant'`，不解析 idle）；② v2 把 V2 状态改判 `unknown` 后，`:670` 在 V2 下**永不成立** → 声称的"信号1 为主"在 V2 下**永久失效**，完成判定实际退化为"信号2 单点 + 超时"；③ 信号1 与信号2 的**布尔关系未定义**（AND/OR），与源码 `:625` 的 `|| streamed` 现状叠加会使 R3 复活。**三者必须一并裁决**：明确 idle 判定的接入函数、明确 `:670` 分支按版本分流、明确双信号布尔关系并消除 `|| streamed`。

### ②.2 B 的 P0-1（V1 分页回归）—— **【我承认遗漏，B 独立成立】**

**任务书问：我报告没提 V1 分页回归，这是否我的遗漏？**

**裁决：是，明确确认为我的独立遗漏，且是架构正确性评审员的职责范围内应覆盖的项。**

- **自证**：我通读 review-arch.md 全文，`P0-C`（:27）与 `P1-E`（:29）**只讨论 V2 侧 order+cursor**与 `listCandidateSessions`。**R1–R13 覆盖表（:74-88）R2 行写"§4.3 分页 ⚠️ 部分（P0-C/P1-E）"——我意识里把"R2 分页"整体归类为 V2 order 问题，从而漏掉了 V1 侧无分页契约这一半。**
- **B 的证据链我复核成立**：`opencode-client.js:368`（`Array.isArray(payload)` 判 V1 形态）、`:146-151`（`unwrap` 对数组原样返回）、`tests/test-host-client.js:65-69`（V1 mock 裸数组）。
- **边界修正（采纳 B battle §②.1 的自我限定基础上，我再加一条）**：B 已自我修正为"条件性 P0"（依赖实现者误读）。我**进一步精确**：V1 分支缺失的**真正硬证据不是"实现者可能误读"，而是 `resolveVersion` 的兜底语义**——复读 `opencode-client.js:116-119`：
  ```js
  async function resolveVersion(baseUrl, timeoutMs = 3000) {
    const v = await detectApiVersion(baseUrl, timeoutMs);
    return v || OPENCODE_API_VERSION.V1;   // 探测失败 → 按 V1 兜底
  }
  ```
  **即：`readAllMessages` 若按单一 V2 信封路径实现，探测失败/超时的 V1 宿主会被强制归入该路径。** 这不是"实现者误读"，是**兜底设计本身的语义**。故我判定 B 的 P0-1 **不是条件性 P0，而是结构性 P0**（比 B 自我限定的更强）。但**此加强不改变结论方向**，且我不因此拆分条数。

### ②.3 B 的 P0-2 与我的 P1-D 是否同一问题？定级谁更准（P0 or P1）？

**任务书问：B 的 P0-2 说「改 unknown 后 V2 永不返回 idle，信号1 永久失效」——这与我的 P1-D 是同一问题吗？定级谁更准？**

**裁决：是同一问题。定级 B 的 P0 更准，我的 P1-D 定级偏低，自我修正为 P0。**

- **同一性**：我 P1-D（review-arch.md:28）原文「把 V2 `getSessionStatus` 不在 active 判为 `unknown` 后，`if(status===idle)`（:670，未改动）在 V2 下永不成立」；B 的 P0-2 第 1 段**表述完全相同**。**这是同一事实的两个署名。**
- **定级裁决 —— B 对，我错**：
  - 我原把 P1-D 定为 P1（"重要"）而非 P0，**理由是"它是 P0-A 的副作用，已被 P0-A 覆盖"**。此理由**成立但不充分**：P0-A 论的是"信号1 无落地点（设计没写）"，P1-D 论的是"信号1 在 V2 下**即使按现设计落地也永不成立**（改 unknown 的直接后果）"。二者是**两个独立失效**：
    - P0-A = 设计缺口（没想到要接线）；
    - P1-D = 接线后仍失效（改了 unknown 把短路杀死）。
  - **后果等级**：改 unknown 是 v2 的**明确设计动作**（:107），它使 V2 完成判定 100% 依赖信号2 + 超时。而超时的下游是 `daemon.js:468-479` 的"超时即 EXTRACTED + 删 fork"→ **直接数据丢失**。**故 P1-D 的后果达 P0 级。**
  - **自我修正**：**P1-D 从 P1 升为 P0**，并入合并条 P0-（完成判定）。**我不为自己原低定级辩解——这是我校准中最明确的一处定级失准。**

### ②.4 P0-C 与 F13 —— **【坚持 + 升级】**

- F13 与我结论一致，**我无损失地确证**。但**我原措辞"若服务端拒绝组合…则翻页拉全整体失效"（review-arch.md:154）是"条件句"，且我在 :168 结论里仍写"或补真机实测"** → 事后看，**spec 的 `cursor` 描述「Do not combine with order」已是明示契约，我本应直接判"确定违背"，不必留"或补实测"的后门**。**自我修正：以后遇到 spec 明示禁令（"Do not"）应直接判违约，不给"待验证"缓冲。**
- **F13 带来的新增事实（我原报告未推演到的）**：翻页只能只带 cursor 且**全程恒 desc（新→旧）**。故 v2 §4.3「显式 `order=asc` 保证"最后一条"语义正确」**根本不可实现**；正确做法是"全程 desc 翻页取第一页第一条"或"翻页取全后内存排序"。**这使 v2 §4.3 的核心手段（order=asc）彻底作废**——比我原 P0-C 的"违背契约"更严重。**接受 B battle §②.5 的升级表述。**

### ②.5 C 的 §7、§8a 是否接受升 P0？

**任务书问：我把 R12 列为「⚠️ 不够具体」，C 升级为 P0。是否接受？**

**裁决：§7（异常路径）与 §8a（既有测试冲突）均接受升 P0；R12 整体（含 sawReply）部分保留 P1。**

- **§7 异常路径 —— 接受升 P0**：复读 `opencode-client.js:576`（`getSessionStatus` 非 ok → `throw`）→ `:664-668`（`try { status = await getSessionStatus(...) } catch { status = 'unknown' }`）→ 401 被吞 → 走超时 → 命中 `daemon.js:468-479`。**且 F1/F2 证明无凭据 `/api/info` 即 401，401 是首要风险**。**我原判"不够具体、不可测"定级偏低（P1→P0），自我修正。**
- **§8a 既有测试冲突 —— 接受升 P0**：F15 确证 `test-host-client-v2.js:152/:198-199` **忠实复刻真机错误行为**。我原报告**完全未提"v2 现有测试会与新设计冲突"**（我的 R4 行只关心"未入 CI"）——**这是我校准中的第二处遗漏**。**接受升 P0**（理由：不显式改写这两条断言，CI 会"为绿而反向固化 bug"，这是**设计-测试反噬**，非普通测试缺口）。
- **R12 的 sawReply 子项 —— 维持 P1**：我原 P1-F 论的是"R9 未定义修法"。复读 `grep sawReply src/` 结果：**仅 3 处，全在 `opencode-client.js` 函数自身（:656/:685/:696），`src/` 内零消费者**。此事实三方无分歧（C §7 亦确认）。它**不直接致数据丢失**（`daemon.js:468` 只读 `completed`），故 **P1 合理，不因 C 升 P0 而在本子项上盲从**。

### ②.6 我的 P2-I 是否遗漏 R8 维度？

**任务书问：我的 P2-I（探测收紧 ✅ 正确）是否遗漏了"探测先带凭据"这一维度？**

**裁决：是，确认为遗漏。但 R8 的定级应为 P1，且我补充一个 B 未提的精确点。**

- **我的 P2-I（review-arch.md:33）全文只论证**「`additionalProperties:false` + `required` 四字段 ⇒ 收紧判据不误伤合法 V2 server」。**我完全未讨论探测的凭据发送顺序。** → **确认遗漏。**
- **复核 B 的代码断言（亲自复读）**：`opencode-client.js:77`：`for (const withAuth of [true, false])` —— **确认为"先带凭据（true）再裸探（false）"**。B 的引用（:77-91）准确。
- **我补充的精确点（B 未提）**：复读 `:84`：`if (res.status === HTTP_OK)` —— **probe 只接受 200**。而 F1 证明**无凭据 → 401**。故：
  - 当前实现下，**带凭据的第一次尝试才会命中 200**；裸探那一次（`withAuth=false`）对 V2 必然拿到 401 而**被跳过**。
  - 即：**现设计下"裸探"这一轮对 V2 是无用功**，而它对外发凭据的目标却是"每一个被 netstat 枚举的本地端口"。
  - **B 建议"先裸探，200 或 401 即命中"需要改 `:84` 的判定条件**（从 `=== HTTP_OK` 改为 `OK || 401`）。**B 的 battle §②.7 只说"先裸探更优"，未点出 `:84` 必须同步改** → 这是**我的独立补充**。
- **定级裁决**：**P1（维持我原报告"R8 遗漏"的隐含等级与 B 一致）**。理由：机制真实（凭据确实先发），但触发前提是"本机存在多个监听端口且其中含恶意/无关服务"，属**安全加固类**，非"必然致数据丢失"。**接受 B 的 P1。**

---

## ③ 去重后的 P0 合并清单（独立裁决）

**任务书要求：B battle 后给 6 条 P0、C battle 后给 2 条真 P0。我在两者间独立裁决：最终几条？为什么？**

### ③.1 裁决结论：**5 条**

**理由（先给判断逻辑，再逐条列举）**：

- **C 的"2 条"过粗**：C 把「设计不自洽」与「数据丢失」各归 1 条，但**把"分页方案不可用"（我 P0-C + B P0-1 + F13）排除在 P0 之外**——C battle §3 实际列了 3 条（P0-1/P0-2/P0-3），**其 §7 自述"2 个真 P0"与其 §3 的 3 条自相矛盾**。且 C 的 3 条中**未含"异常路径零覆盖"与"CI 零覆盖/mock 反固化"**（C 将其降为 P1），而这两项经 F15 确证含**产品级后果**（bug 被 CI 反向固化）。**故 C 的定级在"非设计类"缺口上过松。**
- **B 的"6 条"过细**：B 的 P0-4（failed/interrupted）与 P0-2(a)+(b) 高度交叠（B 自己也承认"相关但独立"，见 battle §②.3）——**P0-4 的落点（`daemon.js:468-479` 布尔无法区分三态）与 P0-3 的落点（`daemon.js:468-479` 超时仍置 EXTRACTED）是同一段代码的同一缺陷**。B 为保"不遗漏"而分列，属**保守性拆分**，非独立缺陷。
- **我的裁决**：按「**同一代码落点 + 同一修复动作 → 合并**」原则，在 B 的 6 条基础上合并 P0-2(a)/(b) 之外的**再合 P0-3+P0-4** → 6-1 = **5 条**；并把 C 的"异常路径"与"CI 反固化"**保留为 P0**（不采纳 C 的降级）。

### ③.2 5 条 P0 清单

| # | P0 名称 | 来源（去重前） | 位置 | 真机/代码证据（本轮亲验） | 放行要求 |
|---|---|---|---|---|---|
| **P0-1** | **§4.3 分页方案双版本均不可落地** | A-P0-C + B-P0-1 + B-P1(升) + C-§3 | `tech-design.md:110-112` | **F13**：`order+cursor` → 400 硬错；翻页**只能只带 cursor 且恒 desc**。**V1 侧**：`opencode-client.js:146-151/368`（裸数组无 cursor），`resolveVersion:118` 探测失败**强制兜底 V1**；方案无 V1 分支 | 改为「只带 cursor 翻页 + 内存取最新」；`readAllMessages` **按 version 分流**（V1 单次裸 GET 不传 limit/order）；给页数/条数硬上限**数值** |
| **P0-2** | **§4.2 完成判定不可落地且内部矛盾（三合一）** | A-P0-A + A-P1-D(升) + B-P0-2(a) + C-§4 | `tech-design.md:95-107` vs `opencode-client.js:623/625/665/670/676` | 信号1 **无接入点**（`:623` 只认 assistant）；`:670` 短路未改动 + 改 `unknown`（:107）⇒ V2 下**永不成立**；双信号**布尔关系未定义**，`:625` 为 `\|\| streamed` ⇒ R3 复活 | 明确 idle 判定**接入函数**；明确 `:670` **按版本分流**（V1 保留短路/V2 改 idle+信号2）；明确双信号 **AND/OR** 并删除 `\|\| streamed` |
| **P0-3** | **失败/超时被误判成功（三态不可区分 + 超时仍置 EXTRACTED + 无条件删 fork）** | A-P0-B + B-P0-2(b) + B-P0-4 + C-R11-2 + C-T6 | `daemon.js:468-479`、`:487-493`；`tech-design.md:100-101` | `if(waited.completed)` **单布尔**无法区分成功/失败/超时；`:475-479` **无条件** EXTRACTED；`:487-493` finally 无条件删 fork ⇒ **数据永久丢失 + 假状态**。OpenAPI：`Idle.outcome` required 三值；真机 F7 **仅观测 succeeded**（failed/interrupted 属规范推断） | 三态差异化：`succeeded`→成功；`failed/interrupted`→**FAILED**；**超时→FAILED 而非 EXTRACTED**（可重试）；必要时改 `waitForSessionIdle` 签名并同步 `daemon.js:468` |
| **P0-4** | **异常路径零覆盖（401/404/500/非 JSON/超时）** | C-§7 + B-P0-5 | `tech-design.md:129-131`；`opencode-client.js:576`→`:666-668` | `:576` 非 ok 抛错 → `:666-668` **吞错转 unknown** → 静默走超时 → 命中 P0-3。**F1/F2：无凭据即 401（首要风险），零测试** | 逐码断言；**401 不得静默吞**；非 JSON 用 `200+HTML` mock 复刻 F2；断言各函数超时生效 |
| **P0-5** | **V2 套件未入 e2e（CI 零覆盖）+ 现有 mock 断言错误行为（CI 反固化）** | C-§6 + C-§8a + **F15** | `tests/e2e.js:20-36`；`tests/test-host-client-v2.js:152/:198-199` | **亲验**：`e2e.js` suites **15 项，无 v2 套件**（:29 仅 test-host-client.js）；`:152` 断言 `'idle'`、`:198-199` 断言 `completed:true`，**与 F10/F11 逐字吻合** ⇒ 修复**必然打红** | suites **显式追加** v2 套件；**显式改写/删除 `:152`、`:198-199`**；修正 `tech-design.md:157` 计数（e2e 实为 15） |

### ③.3 与 B（6 条）/ C（2 条）的差异说明

| 差异 | 我的裁决 | 依据 |
|---|---|---|
| B 的 P0-3（超时即 EXTRACTED）与 P0-4（failed/interrupted）**分立** | **合并为我的 P0-3** | 二者落点同为 `daemon.js:468-479` 的**同一布尔判定**、修复动作为**同一处签名/分支重构**。B battle §②.3 自述"相关但独立"，但"独立"的理由是"管'是否完成' vs 管'完成=成功还是失败'"——**这是同一缺陷的两个观察角，不是两个缺陷**。合并后不丢信息（子项全保留）。 |
| C 把"异常路径"与"CI/mock 反固化"**降为 P1** | **我保留为 P0-4/P0-5** | C 的降级理由（"验证缺口非实现阻塞"）**对 P0-4 成立但对 P0-5 不成立**：F15 证明 mock **会反向固化 bug**（若有人为让 CI 绿而改回旧语义），这是**产品缺陷的固化**，非单纯验证缺口。**故 P0-5 必为 P0。** |
| C 自述"2 条真 P0"与其 §3 列 3 条**不一致** | **以 §3 的 3 条为准，再合并/补入** | C §7 第 1 条称"2 个真 P0（P0-1 完成判定、P0-2 failed/超时）"，但 §3 明确列了 **P0-1/P0-2/P0-3 三条**。**C 自述计数有误。** |
| B 撤回的"25 分钟级联" | **认可撤回，降 P2** | `daemon.js:234` `limit=1` 亲验成立。 |

---

## ④ 我是否维持 REJECTED

**维持 REJECTED。**

**理由（去重后 5 条 P0）**：

1. **P0-1**：v2 §4.3 的**唯一分页手段（`order=asc`）被 F13 真机确证 400 硬拒绝** → 方案即不可落地（非"可能失效"）。且 V1 侧无分支保护，`resolveVersion:118` 的**兜底语义会强制把探测失败的 V1 归入 V2 路径** → V1 抽取静默丢失。
2. **P0-2**：v2 的核心修复目标（"可靠完成判定"）**在 V2 下永久失效**——改 `unknown` 杀死 `:670` 短路，信号1 无接入点，双信号布尔关系未定义且 `:625` 的 `|| streamed` 会使 R3 复活。**方向正确但设计不自洽，实现者必然猜测。**
3. **P0-3**：`daemon.js:468-479` 的单布尔无法区分**成功/失败/超时**，超时仍置 EXTRACTED + 无条件删 fork = **数据永久丢失 + 假状态 + 永不重试**。P0-2 使 V2 **必然走超时**，从而**必然命中**此项 → 修 P0-2 必须连带修此项，否则不闭环。
4. **P0-4**：401 被静默吞（`:576` → `:666-668`）→ 走超时 → 命中 P0-3，而 401 是**真机首要风险**（F1/F2），零测试。
5. **P0-5**：v2 套件未入 CI（亲验 `e2e.js` 15 项无 v2）+ 现有 mock **忠实复刻错误行为**（F15）→ 设计修复必然打红，若不显式改写则 **CI 会反向固化 bug**。这与 P0-4 共同构成"**修复无法被验证、且错误行为被固化**"的闭环风险。

**我的立场变化（诚实记录）**：

- **加严**：
  - 我的 **P1-D 升为 P0**（改 unknown 使 V2 完成判定必然退化 → 数据丢失级后果）。**我原 P1 定级失准，认领。**
  - 接受 **C-§7（异常路径）升 P0**、**C-§8a（mock 反固化）升 P0**。
- **接受遗漏（认领）**：
  - **V1 分页回归**（B-P0-1）——我原报告**零覆盖**，承认是架构评审员职责内的独立遗漏。
  - **R8 凭据外发**——我原 P2-I 只论证探测收紧的判据正确性，**未讨论凭据发送顺序**，承认遗漏。
  - **既有测试冲突**（C-§8a）——我原 R4 行只关心"未入 CI"，**未预见"入了 CI 会红/会反固化"**。
- **放松/撤回**：
  - P0-C 措辞"可能失效"→ **升级为"确定失效"**（F13）。
  - 采纳 B 撤回"25 分钟级联"（`daemon.js:234 limit=1` 亲验）。
- **合并**：我的 P0-A + P1-D + B-P0-2(a) + C-§4 → P0-2；A-P0-C + B-P0-1 + B-P1 → P0-1；A-P0-B + B-P0-2(b) + B-P0-4 + C-R11-2 → P0-3。

**放行门槛（5 条 P0 全修 + 下列 P1 显式表态）**：
- **P1 必须显式表态**：R8（凭据外发，建议改"先裸探"**并同步改 `opencode-client.js:84` 的 `=== HTTP_OK` → `OK || 401`**）、R9（sawReply 死字段修法）、R5（证据计数对齐：e2e 实为 **15** 项）、R13（既有债声明"本次不处理"）、P2-G（agent 黑名单**具体枚举**，且注意现码 `:395` 实为**白名单式拦截**，与 v2 措辞"白名单改黑名单"的现状描述不符）、P2-H（DB 路径无 fork 字段，F14 未解决此项）。
- **真机补验**：**流式中间态**（有 streamed 无 completed，`verification.md §4.1`/F8 仍未抓到）——决定 R3 修复的完备性。

> **附注（不构成裁决）**：v2 的修复**方向**经真机 F1–F15 检验是正确的（idle 消息判完成、分页拉全、fork 字段兜底），证据链扎实。否决的是**方案粒度不足以落地**——F13 已把分页设计判为"确定失效"，F15 证明"忠实复刻错误行为的 mock"会反固化 bug。补齐 5 条 P0 与上述 P1 表态，即可望转 APPROVED。

---

> 证据来源（本轮全部亲自复读，无引用失真）：`tech-design.md:95-112/129-131/157`；`opencode-client.js:77-84/116-119/146-151/368/385-397/564-591/603-640/651-697`；`daemon.js:234/459-479/487-493`；`tests/e2e.js:20-36`；`tests/test-host-client-v2.js:140-199`；`verification.md` F1–F15；`review-arch.md`/`review-risk.md`/`review-test.md`/`battle-risk.md`/`battle-test.md`。
